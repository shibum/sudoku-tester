<?php
define('DEBUG' , true);

define('DEFAULT_CONTROLLER', 'Home'); // default controller if there isn't any.
define('DEFAULT_LAYOUT', 'default'); // if no layout is set in the controller, then use this layout.

define('PROOT', '/sudoku-tester/'); // set this to '/' for live server
define('SITE_TITLE', 'Sudoku Tester'); // use it if no site title is set.
